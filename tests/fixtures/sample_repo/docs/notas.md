# Notas del proyecto de ejemplo

Este fichero markdown no tiene parser dedicado: debe indexarse con el
GenericTextParser (chunking por ventana deslizante), sin aristas.

El carrito calcula totales y descuentos; el checkout calcula el envio.
